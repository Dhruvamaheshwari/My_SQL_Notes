// to create a new database
create database my_database;

// to use the newly created database
use my_database

// to create the table in the new database
create table users(
    id int primary auto_increment not null,
    name varchar(100) not null,
    email varchar(100) unique not null,
    gender enum('male' , 'female' , 'other'),
    date_of_barth date,
    create_at timestamp default current_timestamp
)

// to show the table
select * from users

// to drop the table
drop database my_database


// Rename table name
rename table users to pro

// add some more field in table
alert table users add column isactive default true

// drop the column form the table
alert table users drop column isactive

// modify any field present in the table
alert table users modify column name varchar(150)

// I want to show the email first then name
alert table users modify column email varchar(100) after id;

// __________________CRUD_________________________
//1. insert the vaulus (Create)
// insert into users value
(1 , "Dhruva Maheshwari" , "male" , "dhruva@gmail.com" , "2001-01-01" , default);

// insert into users(id , name ,gender , email , dob ) value  // isme hm n fejle 
(1 , "Dhruva Maheshwari" , "male" , "dhruva@gmail.com" , "2001-01-01" , default);

// insert many data at a time and explicity say this field contain this data
insert into users (id , name , email , gender , date_of_birth) values 
(2 , "honey paajii" , "honey@gmail.com" , "male" , "1990-01-01"),
(3 , "Iron man" , "iron@gamil.com" , "male" , "2000-01-01"),
(4 , "Spider man" , "sider@gmail.com" , "female" , "2006-01-01");


// Read the data
select * from users where gender="female"; 
select * from users where date_of_birth<'1995-09-09';
select * from users where date_of_birth is null;
select * from users where date_of_birth is not null;
select * from users where date_of_birth<"1990-08-08" and gender="female";
select * from users where gender in ("male" , "female");
select * from users where salary between 60000 and 70000;
select * from users where gender="male" or salary>70000;

// asc and desc order
select * from users order by date_of_birth asc; // this is accending order
select * from users order by date_of_birth desc; // this is desinding odrder 

// i want only top 5 rows;
select * from users limit 15;